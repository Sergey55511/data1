import styled from 'styled-components';

export const Wrapper = styled.div`
    .buttonWrapper {
        display: flex;
        justify-content: space-between;
        margin-bottom: 16px;
        .leftButtons {
            display: flex;
            align-items: center;
            column-gap: 16px;
        }
    }
`;

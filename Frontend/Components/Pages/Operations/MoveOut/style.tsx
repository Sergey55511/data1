import styled from 'styled-components';

export const Wrapper = styled.div`
    .header {
        display: flex;
        column-gap: 16px;
        align-items: baseline;
        .recipientWrapper {
            width: 200px;
        }
    }
    .buttonGroup {
        display: flex;
        column-gap: 16px;
        margin-bottom: 8px;
    }
`;

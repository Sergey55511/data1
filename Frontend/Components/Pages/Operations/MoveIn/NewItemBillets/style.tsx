import styled from 'styled-components';

export const Wrapper = styled.div`
    .inputWrapper {
        display: flex;
        column-gap: 16px;
    }
    .addrow {
        margin-bottom: 16px;
    }
    .rows {
    }
`;

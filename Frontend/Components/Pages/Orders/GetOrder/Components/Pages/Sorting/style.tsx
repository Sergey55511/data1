import styled from 'styled-components';

export const Wrapper = styled.div`
    .row {
        display: flex;
        column-gap: 4px;
        padding: 4px;
        border-radius: 4px;
        background-color: RGB(250, 250, 250);
        .item {
            width: 185px;
        }
    }
`;

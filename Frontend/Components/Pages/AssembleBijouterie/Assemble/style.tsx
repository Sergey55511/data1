import styled from 'styled-components';

export const Wrapper = styled.div`
    .header {
        display: flex;
        column-gap: 8px;
        .selectBijouterieWrapper {
            flex: 0 0 200px;
        }
    }
`;

import styled from 'styled-components';

export const Wrapper = styled.div`
    .inputWrapper {
        margin-bottom: 16px;
    }
    .buttonWrapper {
        display: flex;
        column-gap: 8px;
    }
`;

version: "3.9"

services:

  rootapp:
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    image: "kpp2355575/tdata:${APP_VERSION}"
    container_name: rootapp
    ports:
      - "3000:3000"
    env_file:
      - ./docker/app.global.env
      - ./.env
    networks:
      - app_net

  nginx:
    image: nginx
    restart: unless-stopped
    container_name: nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/certs:/etc/nginx/certs
      - ./nginx/conf.d:/etc/nginx/conf.d
    networks:
      - app_net

networks:
  app_net:
    name: app_net
    driver: bridge
